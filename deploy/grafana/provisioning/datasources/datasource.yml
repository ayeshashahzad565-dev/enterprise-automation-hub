# Auto-provisions the Prometheus datasource on Grafana container startup —
# no manual "Add data source" click-through needed.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
